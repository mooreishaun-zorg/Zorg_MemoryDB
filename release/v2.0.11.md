# Zorg MemoryDB v2.0.11

This release corrects the browser LAN Command Chat gauge tile.

- Displays the live `Context window` token count using tokens in use and the
  live token limit.
- Removes the percentage-only gauge readout.
- Derives the visible gauge-tile version from browser package metadata.
- Keeps the version stamp immediately before `DB size`.
